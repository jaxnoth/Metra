# Shared files you can push into sibling projects with:
#   .\metra.ps1 apply .\shared\<file> -RelativePath <path-in-project>
#
# Example:
#   .\metra.ps1 apply .\shared\.editorconfig -RelativePath .editorconfig -Filter '*'
