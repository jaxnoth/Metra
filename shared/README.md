# Shared files you can push into sibling projects with:
#   .\meta.ps1 apply .\shared\<file> -RelativePath <path-in-project>
#
# Example:
#   .\meta.ps1 apply .\shared\.editorconfig -RelativePath .editorconfig -Filter '*'
