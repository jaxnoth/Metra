{
  "version": 1,
  "concepts": [
    {
      "id": "print-queue",
      "tokens": ["print", "queue", "printer", "spooler"],
      "notes": "Print queue / printer monitoring symptoms (PreferProject via local overlay only)"
    },
    {
      "id": "batch-run",
      "tokens": ["batch job", "overnight run", "batch run"],
      "notes": "Generic overnight / batch-job language (no preferred project)"
    }
  ]
}
